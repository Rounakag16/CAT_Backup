{
  "name": "smart-operator-assistant-frontend",
  "version": "0.1.0",
  "private": true,
  "description": "Operator-facing React UI for the Smart Operator Assistant — dashboard, safety banner, idle log, training hub, prediction card.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.4.0"
  },
  "engines": {
    "node": ">=18"
  }
}
